"use client";
import { useEffect, useState } from "react";
import Pagination from "@/components/public/Pagination";
type Folder={id:string;eventName:string;eventDate:string;description:string;coverImage:string;photos:{id:string;url:string}[]};type Result={items:Folder[];page:number;totalPages:number};
export default function GalleryPage(){const [result,setResult]=useState<Result>({items:[],page:1,totalPages:1});const [page,setPage]=useState(1);const [loading,setLoading]=useState(true);useEffect(()=>{setLoading(true);fetch("/api/public/gallery?page="+page).then(r=>r.json()).then(setResult).finally(()=>setLoading(false))},[page]);return <div className="min-h-screen bg-[#f5f7fb] pb-16 pt-20"><header className="bg-[#0B1E3C] px-6 py-16 text-white md:px-10"><div className="mx-auto max-w-7xl"><p className="text-sm font-bold uppercase tracking-[0.2em] text-[#C9A227]">Shared memories</p><h1 className="mt-3 text-4xl font-bold md:text-5xl">Photo gallery</h1></div></header><main className="mx-auto max-w-7xl px-6 py-12 md:px-10">{loading?<p className="text-slate-500">Loading gallery...</p>:result.items.length?<div className="grid gap-7 sm:grid-cols-2 lg:grid-cols-3">{result.items.map(folder=><article key={folder.id} className="overflow-hidden rounded-2xl bg-white shadow-sm"><img src={folder.coverImage||folder.photos[0]?.url||"/images/school3.jpg"} alt="" className="h-64 w-full object-cover"/><div className="p-6"><div className="flex justify-between text-sm text-[#82690d]"><span>{folder.eventDate}</span><span>{folder.photos.length} photos</span></div><h2 className="mt-3 text-xl font-bold text-[#0B1E3C]">{folder.eventName}</h2><p className="mt-2 line-clamp-2 text-sm leading-6 text-slate-600">{folder.description}</p></div></article>)}</div>:<div className="rounded-2xl bg-white p-12 text-center text-slate-500">No gallery collections have been created yet.</div>}<Pagination page={result.page} totalPages={result.totalPages} onPageChange={setPage}/></main></div>;}